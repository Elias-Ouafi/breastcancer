# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
env/
ENV/
.env
.venv
env.bak/
venv.bak/

# IDE
.idea/
.vscode/
.claude/
*.swp
*.swo
.DS_Store

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb

# Project specific
.kaggle/

# The data tree stays out of git: raw DICOM (~80 GB), preprocessed volumes and the
# slice banks derived from them. One rule covers every layer and every future dataset
# -- the previous list of per-experiment folder names had to be edited each time a run
# produced a new directory. Layout and rebuild commands: config.py.
#
# `data/*` rather than `data/`, because git cannot re-include a file whose parent
# directory is excluded, and the demo cases below have to stay versioned.
data/*
!data/curated_data/
data/curated_data/*
!data/curated_data/demo_cases/
data/curated_data/demo_cases/*
# The three curated cases are the exception. Each is a 25-slice slab around one pinned
# axial slice (4-5 MB, against ~30 MB for the full volume) taken from a public, already
# de-identified TCIA study (Duke-Breast-Cancer-MRI), so there is no PHI to leak, and
# shipping them is what makes `git clone` + `pip install` enough to replay the demo.
# The slab, rather than the single slice, is what the slider and the MIP view need.
# Rebuild: scripts/make_demo_cases.py.
!data/curated_data/demo_cases/*.npz

# Logs
*.log
logs/
download.log

# Local development settings
.env.local
.env.development.local
.env.test.local
.env.production.local

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Trained artefacts. Checkpoints are large and most are throwaway, so the tree is
# excluded and the two runs the demo and the README depend on are re-included by name.
models/*

# The DCE-MRI run serving the demo is the exception to "no artefacts in git": its
# checkpoint and metrics ARE versioned, so `git clone` + `pip install` is enough to
# replay the demo on another machine. Everything else in the folder stays out.
!models/dce_mri_p2_negfix/
models/dce_mri_p2_negfix/*
!models/dce_mri_p2_negfix/unet_best.pt
!models/dce_mri_p2_negfix/segmentation_metrics.csv
!models/dce_mri_p2_negfix/segmentation_test_metrics.csv
!models/dce_mri_p2_negfix/eval_report.json
!models/dce_mri_p2_negfix/eval_per_patient.csv

# Slice classifier (imaging.sliceclf): the checkpoint is 2.4 MB and its metrics are
# the evidence behind the localisation claims, so both are versioned.
!models/sliceclf/
models/sliceclf/*
!models/sliceclf/sliceclf_best.pt
!models/sliceclf/sliceclf_metrics.csv
!models/sliceclf/sliceclf_test_metrics.json

# Lesion classifier (imaging.lesionclf): the reports, not the checkpoint. The reports
# are the evidence for a *negative* result (plan.md section 4.5) and are 5 KB; the
# checkpoint is a model that does not separate the two classes, and versioning it
# would invite someone to serve it.
!models/lesionclf/
models/lesionclf/*
!models/lesionclf/cv_report.json
!models/lesionclf/cv_report_stretched.json
!models/lesionclf/cv_predictions.csv

# Exam classifier (imaging.examclf): same reasoning as lesionclf just above -- the
# reports are the evidence for a *negative* result (plan.md section 4.7), the
# checkpoint is a model that detects none of its 56 held-out cancers.
!models/examclf/
models/examclf/*
!models/examclf/cv_report.json
!models/examclf/cv_predictions.csv

# The tabular model, flattened by tabular_export: 13 KB of JSON, and the only copy
# that can be served without a JVM. Versioned for the same reason as the checkpoints
# above -- /biopsie has to answer on a fresh clone, and refitting it needs Spark,
# Java and a network fetch of the UCI dataset. The Spark PipelineModel it was
# exported from stays out: it is a directory of parquet only Spark can read, and
# nothing in the serving path opens it.
!models/tabular/
models/tabular/*
!models/tabular/tabular_model.json
!models/tabular/metadata.json
